<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record model="ir.ui.view" id="minver_purchase_order_form">
            <field name="name">minver.purchase.order.form</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_form"/>
            <field name="arch" type="xml">
                <field name="partner_id" position="before">
                    <field name="oc_sap"/>
                    <field name="imports"/>
                </field>
                <xpath expr="//notebook" position="inside">
                    <page name="import" string="Importación" attrs="{'invisible': [('imports', '=', False)]}">
                        <group>
                            <group>
                                <field name="awb_bl"/>
                                <field name="cont_nbr"/>
                                <field name="cia_id"/>
                                <field name="mn_id"/>
                            </group>
                            <group>
                                <field name="arrival_port_id"/>
                                <field name="planned_wh_id"/>
                                <field name="warehouse_id"/>
                            </group>
                        </group>
                    </page>
                    <page name="checklist" string="Checklist">
                        <field name="checklist_line" widget="section_and_note_one2many" mode="tree,kanban">
                            <tree string="checklist_line" editable="bottom">
                                <field name="name"/>
                                <field name="state"/>
                                <field name="task_eta"/>
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>

        <record id="purchase_order_line_view_tree" model="ir.ui.view">
            <field name="name">purchase.order.line.view.tree</field>
            <field name="model">purchase.order.line</field>
            <field name="arch" type="xml">
                <tree
                    decoration-muted="state=='cancel'"
                    decoration-info="state in ('wait','confirmed')"
                    string="Purchase Order"
                    class="o_purchase_order"
                    sample="1">
                    <field name="name_order" string="Reference" readonly="1" decoration-bf="1"/>
                    <field name="date_order" widget="date" invisible="context.get('quotation_only', False)" optional="show"/>
                    <field name="partner_id"/>
                    <field name="date_planned" invisible="context.get('quotation_only', False)" optional="show"/>
<!--                    <field name="user_id"/>-->
<!--                    <field name="origin"/>-->
                    <field name="product_id"/>
<!--                    <field name="name"/>-->
                    <field name="product_qty"/>
                    <field name="product_uom"/>
<!--                    Importacion-->
                    <field name="awb_bl"/>
                    <field name="cont_nbr"/>
                    <field name="cia_id"/>
                    <field name="mn_id"/>
                    <field name="arrival_port_id"/>
                    <field name="planned_wh_id"/>
                    <field name="warehouse_id"/>

<!--                    <field name="price_unit"/>-->
<!--                    <field name="taxes_id"/>-->
<!--                    <field name="price_subtotal"/>-->
<!--                    <field name="invoice_status" widget="badge" decoration-success="invoice_status == 'invoiced'" decoration-info="invoice_status == 'to invoice'" optional="show"/>-->
                    <field name="state" invisible="1"/>
                </tree>
            </field>
        </record>

        <record id="purchase_line_form_action" model="ir.actions.act_window">
            <field name="name">Purchase Orders Lines</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">purchase.order.line</field>
            <field name="view_mode">tree,kanban,form</field>
            <field name="view_id" ref="purchase_order_line_view_tree"/>
            <field name="domain">[('state','in',('purchase', 'done'))]</field>
<!--            <field name="search_view_id" ref="purchase_order_view_search"/>-->
            <field name="context">{}</field>
            <field name="help" type="html">
              <p class="o_view_nocontent_smiling_face">
                No purchase order found. Let's create one!
              </p><p>
                Once you ordered your products to your supplier, confirm your request for quotation and it will turn into a purchase order.
              </p>
            </field>
        </record>
        <menuitem action="purchase_line_form_action" id="menu_purchase_form_action" parent="purchase.menu_procurement_management" sequence="6"/>

    </data>
</odoo>
